[project]
name = "isabelle"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiohttp>=3.13.0",
    "blockkit>=2.0.4",
    "piccolo-admin>=1.12.0",
    "piccolo[postgres]>=1.28.0",
    "python-dateutil>=2.9.0",
    "python-dotenv>=1.0.1",
    "requests>=2.32.3",
    "schedule>=1.2.2",
    "sentry-sdk>=2.23.1",
    "slack-bolt>=1.23.0",
    "slack-gfm>=0.2.1",
    "starlette>=0.48.0",
    "tzdata>=2025.1",
    "uvicorn>=0.37.0",
]

[project.scripts]
isabelle = "isabelle.__main__:start"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pre-commit>=4.1.0",
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
    "httpx>=0.28.0",
    "ruff==0.9.6",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
markers = [
    "db: requires a live Postgres with migrations applied",
]
